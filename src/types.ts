export interface Product {
  id: number;
  brand: string;
  name: string;
  price: number;
  offer: number;
  description: string;
  img: string;
  category: string;
}

export type StringTouple = [string, string];
